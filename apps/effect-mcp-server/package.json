{
  "name": "@apps/effect-mcp-server",
  "version": "0.0.0",
  "private": true,
  "description": "Effect MCP Server Example App",
  "license": "UNLICENSED",
  "type": "module",
  "main": "build/index.js",
  "files": [
    "build"
  ],
  "scripts": {
    "build": "vite build",
    "clean": "del .turbo coverage build *.sarif",
    "dev": "node --watch --env-file=.env src/index.ts",
    "lint": "eslint ",
    "mcp-inspector": "mcp-inspector node src/main.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@effect/platform-node": "4.0.0-rc.112",
    "effect": "4.0.0-rc.112"
  },
  "devDependencies": {
    "@modelcontextprotocol/inspector": "2.4.0",
    "@toolchain/eslint": "workspace:*",
    "@toolchain/typescript": "workspace:*",
    "@toolchain/vite": "workspace:*",
    "@toolchain/vitest": "workspace:*",
    "@types/node": "26.4.0"
  }
}
